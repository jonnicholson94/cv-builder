
import { screen, render, fireEvent } from "@testing-library/react"
import "@testing-library/jest-dom"

import DashboardEducationForm from "./DashboardEducationForm"

test.todo("Updates course state when input typed in")

test.todo("Updates institution state when input typed in")

test.todo("Updates description state when input typed in")

test.todo("Updates start date state when select clicked")

test.todo("Updates end date state when select clicked")

test.todo("Handles submit when button clicked")

test.todo("Handles delete when button clicked")